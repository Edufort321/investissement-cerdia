/**
 * Layout dedie pour /onboarding : pas de navbar publique, pas de sidebar.
 * Page wizard plein écran pour la 1ere configuration du tenant.
 */

export default function OnboardingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
